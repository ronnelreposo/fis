<UserControl x:Class="FIS.Pages.Logout"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="400" d:DesignWidth="600">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="60"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <TextBlock Text="Do you really want to exit this program?" Grid.Row="0" Grid.ColumnSpan="4" VerticalAlignment="Bottom" HorizontalAlignment="Center" FontSize="25"/>
        <Button x:Name="logoutButton" Content="LOGOUT" Grid.Row="1" Grid.Column="1" Margin="10" Click="logoutButton_Click" />
        <Button x:Name="cancelButton" Content="CANCEL" Grid.Row="1" Grid.Column="2" Margin="10" Click="cancelButtom_Click" IsCancel="True" AutomationProperties.HelpText="HELLO"/>

    </Grid>
</UserControl>
